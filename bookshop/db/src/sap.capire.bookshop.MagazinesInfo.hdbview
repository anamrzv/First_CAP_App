VIEW SAP_CAPIRE_BOOKSHOP_MAGAZINESINFO (in REQ_RATING integer) AS SELECT
  ID,
  RATING,
  'From my HANA view: The magazine ' || TITLE || ' is published by ' || PUBLISHER AS MAGAZINE_PUBLISHER_INFO
FROM SAP_CAPIRE_BOOKSHOP_MAGAZINES
WHERE RATING = :REQ_RATING;